CREATE TABLE PATIENT (ID INTEGER NOT NULL, PRIMARY KEY (ID))
CREATE TABLE DATA (ID BIGINT NOT NULL, FILE VARCHAR(255), FROM_TIMESTAMP TIMESTAMP, TO_TIMESTAMP TIMESTAMP, TYPE VARCHAR(255), THERAPYRESULT_ID BIGINT, PRIMARY KEY (ID))
CREATE TABLE THERAPY (ID BIGINT NOT NULL, PRIMARY KEY (ID))
CREATE TABLE THERAPYRESULT (ID BIGINT NOT NULL, CAPTION VARCHAR(255), COMMENT VARCHAR(255), SUCCESS INTEGER, TIMESTAMP DATE, DATA_ID BIGINT, PRIMARY KEY (ID))
CREATE TABLE SENSOR (ID BIGINT NOT NULL, PRIMARY KEY (ID))
ALTER TABLE DATA ADD CONSTRAINT DTATHERAPYRESULTID FOREIGN KEY (THERAPYRESULT_ID) REFERENCES THERAPYRESULT (ID)
ALTER TABLE THERAPYRESULT ADD CONSTRAINT THERAPYRESULTDTAID FOREIGN KEY (DATA_ID) REFERENCES DATA (ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(15), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(15), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
